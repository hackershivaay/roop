# Torch GPU support (for CUDA 11.8)
--extra-index-url https://download.pytorch.org/whl/cu118

# Core dependencies
numpy==1.26.1
opencv-python==4.8.0.74
onnx==1.14.0
onnxruntime-gpu==1.21.0
insightface==0.7.3
psutil==5.9.5
pillow==10.0.0
protobuf==4.23.4
tqdm==4.65.0
gfpgan==1.3.8
opennsfw2==0.10.2
keras>=2.13.0

# Optional GUI packages (for local use, not needed in Colab)
# These can be commented out if the tool runs purely on Colab
# tk==0.1.0
# customtkinter==5.2.0
# tkinterdnd2==0.3.0
# tkinterdnd2-universal==1.7.3

# ONNX runtime conditions (simplified for Colab/Linux use)
# (you don’t need Apple-specific versions here)
